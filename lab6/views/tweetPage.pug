html(lang="en")
    head
        style
            include style1.css
    body
        h2.header Tweet
        block content
            table
                each post in tweet_
                    lu.containerTweet
                        li Name - #{post.name}
                        li Username - #{post.username}
                        li text - #{post.text}
                        li likes - #{post.likes}
                        img.tweet(src=post.image)